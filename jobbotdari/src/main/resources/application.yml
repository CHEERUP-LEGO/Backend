spring:
  application:
    name: jobbotdari
  config:
#    import: "optional:configserver:http://localhost:8071"
    import: "optional:configserver:http://config-server-8071-dns:8071"
  rabbitmq:
    host: 3.143.42.91
    port: 5672
    username: guest
    password: guest

management:
  endpoints:
    web:
      exposure:
        include: refresh, health, beans, httptrace, busrefresh